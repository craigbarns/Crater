{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "touch .env && chmod 777 .env && chmod -R 777 storage bootstrap/cache && php artisan optimize && php -S 0.0.0.0:$PORT -t public",
        "restartPolicyType": "ON_FAILURE"
    }
}